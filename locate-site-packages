#!/bin/bash
if [ -z "$1" ]; then
	echo "Please provide the python executable for which you want to find"
	echo "the site-packages directory."
	exit
fi

$1 -c 'from distutils.sysconfig import get_python_lib; print get_python_lib()'
